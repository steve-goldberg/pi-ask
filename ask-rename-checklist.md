# Ask Rename Execution Checklist

Status: completed

Scope: naming only. No functionality changes.

## Completed

- [x] Renamed top-level planning files to `ask-plan.md` and `ask-progress.json`
- [x] Renamed the extension directory to `.pi/extensions/ask/`
- [x] Renamed all ask-related test files under `tests/unit/` and `tests/integration/`
- [x] Updated docs in `README.md`, `PRD.md`, `progress.json`, `ask-progress.json`, and `ask-plan.md`
- [x] Updated extension symbols, strings, paths, and prompts under `.pi/extensions/ask/`
- [x] Updated command and tool naming to `/ask` and `ask`
- [x] Updated draft storage naming to `.pi/tmp/ask.json`
- [x] Updated test imports, expectations, temp file names, and temp directory prefixes
- [x] Ran verification commands successfully:
  - [x] `npm run lint`
  - [x] `npm run typecheck`
  - [x] `npm test`

## Result

The repo is now named and referenced as **ask** in the active project files.
